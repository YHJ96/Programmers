```SQL
-- 코드를 입력하세요
-- NAME과 ANIMAL_ID 속성만 선택
SELECT NAME, ANIMAL_ID
-- 테이블명 기술
FROM ANIMAL_INS
-- INTAKE_CONDITION이 "Sick"인 동물만 선택
WHERE INTAKE_CONDITION = "Sick"
-- ANIMAL_ID 기준 오름차순 정렬
ORDER BY ANIMAL_ID
```